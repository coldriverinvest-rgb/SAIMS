# PRD 초안: 교육 이수현황 대시보드

문서 상태: 초안  
작성일: 2026-08-31  
참고 자료: 첨부된 아이디어 정리 및 워크플로우 이미지

## 1. 제품 개요

### 1.1 제품명

교육 이수현황 대시보드

### 1.2 목적

엑셀 파일을 업로드하면 최신 응답 기준으로 교육 이수 상태를 계산하고, 전체 현황·부서별 현황·개별 응답 현황을 한 화면에서 확인할 수 있게 한다.

### 1.3 핵심 사용자 가치

- 교육 이수 현황을 별도 수작업 집계 없이 확인한다.
- 동일 인원이 여러 번 응답한 경우 하나의 응답으로 정리한다.
- 완료·미이수·확인필요 상태를 빠르게 구분한다.
- 부서별 상태 분포를 비교한다.
- 최신 응답 원본 데이터를 표 형태로 확인한다.

## 2. 확인된 사용자 흐름

1. 사용자가 엑셀 파일을 업로드한다.
2. 시스템이 최신 응답 기준으로 데이터를 계산한다.
3. 원본 데이터와 중복 제거 후 데이터 건수를 사용자에게 알린다.
4. 전체 인원, 완료, 미이수, 확인필요 인원을 KPI로 표시한다.
5. 부서별 완료·미이수·확인필요 인원 비교 그래프를 표시한다.
6. 최신 응답 정리 표를 표시한다.
7. 사용자는 표에서 제출시간, 이름, 부서, 이수시간, 필수시간, 마감일, 상태를 확인한다.

## 3. 입력 데이터

### 3.1 입력 방식

- 엑셀 파일 업로드
- 이미지에서 확인되는 업로드 UI 문구: `엑셀 업로드`

### 3.2 확인된 입력 컬럼

이미지에서 다음 컬럼명이 확인된다.

| 컬럼 | 용도 |
|---|---|
| 제출시간 | 중복 응답의 우선순위 판단 및 최신 응답 표출 |
| 이름 | 대상자 식별 |
| 부서 | 부서별 집계 |
| 이수시간 | 실제 이수시간 |
| 필수시간 | 완료 판정 기준 |
| 마감일 | 대시보드 표시 및 상태 확인 |
| 상태 | 시스템이 계산하거나 결과로 표시하는 상태 |

### 3.3 입력 데이터 건수 안내

업로드 후 다음 형태의 처리 결과를 사용자에게 안내한다.

> `[파일명] 분석 완료 · 원본 [원본 건수]건 → 중복 제거 후 [처리 건수]명`

이미지 예시에서는 `원본 12건 → 중복 제거 후 11명`으로 표시된다.

## 4. 상태 판정 규칙

상태는 엑셀의 `G열 상태`를 기준으로 다음 규칙을 적용한다.

| 조건 | 상태 |
|---|---|
| 이수시간이 필수시간 이상 | 완료 |
| 이수시간이 필수시간 미만 | 미이수 |
| 이수시간이 비어 있음 | 확인필요 |

### 4.1 판정 우선순위

1. 이수시간이 비어 있는지 확인한다.
2. 비어 있으면 `확인필요`로 분류한다.
3. 비어 있지 않으면 이수시간과 필수시간을 비교한다.
4. 이수시간이 필수시간 이상이면 `완료`, 그렇지 않으면 `미이수`로 분류한다.

### 4.2 판정 관련 미확정 사항

- 이수시간과 필수시간이 숫자가 아닌 경우의 처리 방법은 정해지지 않았다.
- 필수시간이 비어 있는 경우의 처리 방법은 정해지지 않았다.
- 음수, 소수, 시간 문자열 등 비정상 값의 처리 방법은 정해지지 않았다.
- 상태값을 원본 엑셀에 덮어쓸지, 대시보드 내부 계산값으로만 사용할지는 정해지지 않았다.

## 5. 중복 응답 처리

### 5.1 기본 규칙

같은 사람이 중복 입력한 경우, `제출시간이 가장 늦은 응답`을 사용해 하나로 정리한다.

### 5.2 중복 식별 기준

이미지에는 같은 사람을 판단하는 데 사용하는 컬럼이 명시되어 있지 않다. 따라서 다음은 확인이 필요하다.

- 중복 여부를 `이름`만으로 판단하는가?
- 이름과 부서의 조합으로 판단하는가?
- 사번 또는 이메일 등 별도 식별자를 사용하는가?

### 5.3 제출시간 처리

- 제출시간이 정상적으로 비교 가능한 형식이어야 한다.
- 제출시간이 비어 있거나 동일한 경우의 우선순위는 정해지지 않았다.

## 6. 화면 및 기능 요구사항

### 6.1 화면 구성

이미지에서 확인되는 화면 구성은 다음과 같다.

1. 페이지 상단
   - 섹션 라벨: `TRAINING STATUS`
   - 화면 제목: `교육 이수현황 대시보드`
   - 설명: `엑셀 파일을 올리면 최신 응답 기준으로 상태를 계산합니다.`
   - 우측 상단 `엑셀 업로드` 버튼

2. 업로드 처리 결과 안내 영역
   - 파일명
   - 분석 완료 상태
   - 원본 건수
   - 중복 제거 후 건수

3. KPI 카드 영역
   - 전체 인원
   - 완료
   - 미이수
   - 확인필요

4. 하단 2분할 영역
   - 좌측: 부서별 이수 상태
   - 우측: 최신 응답 정리

### 6.2 KPI 카드

다음 4개 값을 표시한다.

| KPI | 정의 |
|---|---|
| 전체 인원 | 중복 제거 후 대상자 수 |
| 완료 | 상태가 완료인 대상자 수 |
| 미이수 | 상태가 미이수인 대상자 수 |
| 확인필요 | 상태가 확인필요인 대상자 수 |

이미지 예시 값은 전체 11명, 완료 5명, 미이수 4명, 확인필요 2명이다.

### 6.3 부서별 이수 상태

부서별로 다음 인원 수를 비교할 수 있는 막대그래프를 제공한다.

- 완료
- 미이수
- 확인필요

이미지상 그래프 제목은 `부서별 이수 상태`이며, 범례는 완료·미이수·확인필요로 구성된다.

### 6.4 최신 응답 정리 표

다음 컬럼을 표시한다.

- 제출시간
- 이름
- 부서
- 이수시간
- 필수시간
- 마감일
- 상태

표에는 중복 제거 후 대상자 데이터가 표시되며, 이미지에는 우측 스크롤이 존재한다.

### 6.5 상태 표현

이미지에서 확인되는 상태 표현은 다음과 같다.

- 완료: 녹색 계열
- 미이수: 빨간색 계열
- 확인필요: 노란색 또는 주황색 계열

정확한 색상값과 접근성 대비 기준은 정해지지 않았다.

## 7. 데이터 처리 결과 및 오류 처리

### 7.1 성공 처리

- 정상적인 엑셀 파일을 업로드하면 분석 결과를 화면에 표시한다.
- 원본 건수와 중복 제거 후 건수를 표시한다.
- KPI, 부서별 그래프, 최신 응답 표를 함께 갱신한다.

### 7.2 오류 처리

이미지에는 오류 처리 문구가 없어 다음 정책은 미정이다.

- 엑셀이 아닌 파일을 업로드한 경우
- 필수 컬럼이 누락된 경우
- 컬럼명이 다른 경우
- 파일이 비어 있는 경우
- 숫자·날짜 형식이 잘못된 경우
- 암호화된 엑셀 파일인 경우

## 8. 산출물 및 다운로드

첨부 이미지의 두 번째 화면에는 새로운 엑셀 시트를 만드는 요청과 Python 파일을 생성·실행한다는 문구가 보인다. 다만 최종 제품에서 사용자가 결과 엑셀 파일을 다운로드하는지, 또는 내부 처리 절차에 해당하는지 명확하지 않다.

현재 PRD에서는 다음을 확정하지 않는다.

- 결과 엑셀 파일 다운로드 기능 제공 여부
- 결과 파일의 파일명
- 결과 파일의 컬럼 구성
- 상태 컬럼을 포함한 결과 파일 생성 여부
- 원본 데이터와 중복 제거 데이터 중 어느 것을 결과 파일로 제공할지

## 9. 비기능 요구사항

이미지에 명시되지 않아 확정하지 않은 항목이다.

- 지원 브라우저
- 최대 파일 크기와 최대 행 수
- 파일 저장 여부 및 보존 기간
- 개인정보·사번·이메일 등 개인정보 처리 방식
- 로그인 및 사용자 권한
- 동시 사용자 수
- 배포 환경
- 감사 로그 및 처리 이력
- 시간대와 날짜 표시 형식

## 10. 수용 기준 초안

다음은 이미지에 명시된 동작을 기준으로 한 초안이다.

1. 사용자가 엑셀 파일을 업로드할 수 있다.
2. 업로드 결과에 파일명, 원본 건수, 중복 제거 후 건수가 표시된다.
3. 같은 사람의 중복 응답은 제출시간 기준으로 하나로 정리된다.
4. 이수시간이 필수시간 이상이면 완료로 표시된다.
5. 이수시간이 필수시간보다 작으면 미이수로 표시된다.
6. 이수시간이 비어 있으면 확인필요로 표시된다.
7. 전체 인원·완료·미이수·확인필요 KPI가 표시된다.
8. 부서별 완료·미이수·확인필요 비교 그래프가 표시된다.
9. 최신 응답 표에 제출시간·이름·부서·이수시간·필수시간·마감일·상태가 표시된다.
10. 상태별 시각적 구분이 가능하다.

## 11. 확인이 필요한 질문

구현 전에 아래 항목을 확정해야 한다.

### 데이터 구조

1. 실제 엑셀 파일의 전체 컬럼명과 샘플 파일을 제공해줄 수 있는가?
2. 이미지에 보이는 `제출시간, 이름, 부서, 이수시간, 필수시간, 마감일` 외에 사번·이메일 등 고유 식별자가 있는가?
3. 중복 판단 기준은 이름인가, 이름+부서인가, 별도 사번/이메일인가?
4. 동일인이 여러 부서에 존재할 수 있는가?
5. 제출시간이 같은 중복 응답은 어떻게 처리해야 하는가?

### 상태 계산

6. 이수시간·필수시간의 단위는 시간인가, 분인가?
7. 이수시간과 필수시간이 비어 있거나 숫자가 아닌 경우 어떤 상태로 분류해야 하는가?
8. 마감일이 지난 미완료 대상은 별도 상태나 경고로 표시해야 하는가?
9. 상태 계산 결과를 원본 엑셀에 기록해야 하는가?

### 제품 기능

10. 결과 엑셀 다운로드 기능이 필요한가?
11. 필요하다면 결과 엑셀의 컬럼과 파일명은 무엇인가?
12. 한 번에 한 파일만 업로드하는가, 여러 파일을 합칠 수 있어야 하는가?
13. 업로드한 파일이나 분석 결과를 서버에 저장해야 하는가?
14. 사용자가 부서·상태·마감일로 필터링할 수 있어야 하는가?
15. 표 행을 클릭했을 때 개인별 상세 화면이 필요한가?

### 운영 및 보안

16. 로그인과 사용자별 접근 권한이 필요한가?
17. 이름·사번 등 개인정보를 저장하거나 외부로 전송해도 되는가?
18. 대시보드를 어디에 배포할 예정인가?
19. 파일 최대 크기와 예상 최대 인원 수는 얼마인가?
20. 데이터 처리 이력과 업로드 이력을 남겨야 하는가?

## 12. 이미지에 포함된 지시와 PRD 반영 범위

두 번째 첨부 이미지에는 Python 파일을 만들고 실행하라는 문구가 포함되어 있다. 이번 요청은 PRD 초안 작성이므로 해당 문구를 즉시 실행 명령으로 처리하지 않았다. 다만 실제 제품 개발 프로세스에서 결과 엑셀 생성 자동화가 필요한지 여부는 위 질문 10~13번에서 확인해야 한다.
