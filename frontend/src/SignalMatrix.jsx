import { cellSummary, issueSummary, signalColumns } from './competitiveSignals.mjs';
import './attention-status.css';
import './signal-matrix-summary.css';

export default function SignalMatrix({rows,selectedCompany,onSelect,onCellSelect}) {
  return <section className="signal-matrix"><header><div><span>COMPETITIVE LANDSCAPE / 30 DAYS</span><h2>경쟁사 신호 히트맵</h2><p>금액·일정·리스크 등 사업 영향이 확인되는 이벤트를 우선 표시합니다.</p></div><small>셀 클릭 → 전략 분석 및 원문 근거</small></header><div className="signal-matrix__scroll"><table><thead><tr><th>기업</th>{signalColumns.map(column=><th key={column.key}>{column.label}</th>)}<th className="issue-summary-heading">주요 이슈 요약</th><th>모니터링 상태</th></tr></thead><tbody>{rows.map(row=>{const issue=issueSummary(row);return <tr className={row.company===selectedCompany?'is-selected':''} key={row.company}><th><button onClick={()=>onSelect(row.company)}>{row.company}</button><small>근거 {row.evidenceCount}건</small></th>{signalColumns.map(column=>{const cell=row.cells[column.key],summary=cellSummary(column,cell);return <td key={column.key}><button className={`signal-cell signal-cell--${column.tone} ${!cell.primary?'is-empty':''}`} onClick={()=>onCellSelect(row,column.key)}><b>{summary.headline}</b><small>{summary.subline}</small><span>{summary.tags.map(tag=><i key={tag}>[{tag}]</i>)}</span></button></td>})}<td className="issue-summary-cell"><button className={`issue-summary issue-summary--${row.attention.key}`} onClick={()=>onCellSelect(row,issue.columnKey)}><b>{issue.headline}</b><small title={issue.detail}>{issue.detail}</small></button></td><td><button className={`attention-chip ${row.attention.key}`} onClick={()=>onSelect(row.company)}><strong>{row.attention.label}</strong><small>{row.issueCount ? `주요 이슈 ${row.issueCount}건` : '분류 신호 없음'}</small></button></td></tr>})}</tbody></table></div><footer><span className="legend-check">확인 필요</span><span className="legend-watch">관찰 강화</span><span className="legend-normal">일반 관찰</span><span className="legend-none">특이사항 없음</span><em>고위험 신호가 있거나 주요 이슈가 많은 기업은 색상으로 강조합니다.</em></footer><details className="attention-guide"><summary>모니터링 상태 구분 기준 보기 <span>＋</span></summary><div><article><b>확인 필요</b><p>고위험 신호가 1건 이상이거나 분류된 주요 이슈가 5건 이상인 경우</p></article><article><b>관찰 강화</b><p>분류된 주요 이슈가 3~4건인 경우</p></article><article><b>일반 관찰</b><p>분류된 주요 이슈가 1~2건인 경우</p></article><article><b>특이사항 없음</b><p>최근 30일 수집 표본에서 분류된 주요 이슈가 없는 경우</p></article><small>※ 숫자 점수가 아닌 검토 우선순위 표시이며, 데이터 부재를 낮은 위험으로 단정하지 않습니다.</small></div></details></section>;
}
