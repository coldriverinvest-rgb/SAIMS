import { executiveBriefing } from './competitiveSignals.mjs';
export default function ExecutiveSummaryPanel({ row, onSelect }) { const lines=executiveBriefing(row); return <aside className="executive-panel"><div className="executive-panel__head"><span>COMPANY INTELLIGENCE</span><select aria-label="분석 기업 선택" value={row?.company||''} onChange={e=>onSelect(e.target.value)}>{row?.allRows?.map(item=><option key={item.company}>{item.company}</option>)}</select></div><div className={`attention-hero ${row?.attention?.key||'none'}`}><span>MONITORING STATUS</span><strong>{row?.attention?.label||'특이사항 없음'}</strong><small>{row?.attention?.description}</small></div><div className="attention-counts"><p><b>{row?.issueCount||0}건</b> 주요 이슈</p><p><b>{row?.highRiskCount||0}건</b> 고위험 신호</p></div><p className="threat-note">최근 30일 수집 표본에서 분류된 주요 이슈 수와 고위험 신호 유무로 구분합니다.</p><section className="executive-brief"><h3>AI EXECUTIVE BRIEFING</h3>{lines.map((line,index)=><p key={line}><b>0{index+1}</b>{line}</p>)}</section></aside>; }
